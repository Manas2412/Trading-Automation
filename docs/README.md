# Design Docs

- **HLD.md** — high-level design: architecture, data flow, deployment, boundaries. *(in progress)*
- **LLD.md** — low-level design: module interfaces, schemas, API contracts, concurrency & locking. *(in progress)*
- **adr/** — architecture decision records (one file per significant decision).

Order of authoring: HLD → LLD → then `.claude/` agents & project skills reflect the finalized design.
