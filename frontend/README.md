# frontend

Placeholder for the qtrade dashboard/UI (React.js — production frontend per ADR-0001/HLD).
Streamlit may serve as an interim dashboard first. Generate the React app here later.

It will talk to the backend's `ops` API; it never holds broker or AWS credentials.
